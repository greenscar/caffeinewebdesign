import HTML.*;

public class SearchCell extends HTMLTableCell {
    
    /** Creates a new instance of SearchCell */
    public SearchCell() {
        HTMLHeading heading = new HTMLHeading("Title Search", HTMLHeading.H4);
        heading.setAlignment(CENTER);
        addObject(heading);
        addObject(new SearchForm());
    }
}
